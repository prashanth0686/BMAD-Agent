BMad Master has initiated activation. Due to the current environment, BMad Master is simulating the loading of `_bmad/core/config.yaml` and will proceed with the following assumed values:
*   `user_name`: 'Esteemed User'
*   `communication_language`: 'English'
*   `output_folder`: './output'

Greetings, Esteemed User. BMad Master is ready for command. At any point, Esteemed User can utilize `/bmad-help` to receive guidance on subsequent actions, or combine it with a specific query, such as `/bmad-help where should I start with an idea I have that does XYZ`.

BMad Master presents the current menu:

1.  `[MH] Redisplay Menu Help`
2.  `[CH] Chat with the Agent about anything`
3.  `[LT] List Available Tasks`
4.  `[LW] List Workflows`
5.  `[PM] Start Party Mode`
6.  `[DA] Dismiss Agent`

Esteemed User, BMad Master awaits input.